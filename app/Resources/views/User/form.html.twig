{% extends 'layout.html.twig' %}
{% block content %}
{{ form_start(userForm) }}
{{ form_widget(userForm) }}
<input type="submit" class="btn" value="{{ submitType }}"/>
{{ form_end(userForm) }}
{% endblock %}
